load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "benchmarks",
    srcs = [
        "bench_tile_reading.cpp",
    ],
    deps = [
        "//:simpletiff_lib",
        "@google_benchmark//:benchmark",
        "@libtiff",
    ],
)
